# 10주 이후 — 다음 단계

10주 커리큘럼은 "FAQ 챗봇"이라는 구조화된 데이터와 단순한 시나리오로 시작했다.
의도적인 선택이었다 — 개념을 익히기에는 단순한 환경이 낫다.

하지만 실무 AI 시스템이 마주하는 진짜 문제는 다른 곳에 있다.

---

## 10주 커리큘럼의 한계

이 커리큘럼 내내 고급 검색 전략(ReRanker, QueryTransform)이 Baseline보다 오히려 나빴다.
이유는 데이터 구조에 있었다:

```
FAQ 문서 (구조화)
  └─ TokenTextSplitter
      └─ 17개 대형 청크 (청크당 여러 Q&A 혼재)
          └─ ReRanker: 500자 잘라냄 → 정답 Q&A가 날아감 → 역효과
```

벡터 검색만으로도 충분한 환경에서 고급 전략을 배웠다.
교훈은 얻었지만, "고급 전략이 실제로 필요한 환경"을 경험하지 못했다.

다음 단계는 그 환경을 직접 만드는 것이다.

---

## 다음 도전: 비구조화 데이터 RAG

### 시나리오

지금까지는 FAQ처럼 **이미 정돈된 문서**를 검색했다.
실무에서 더 자주 마주치는 상황은 이것이다:

```
채팅 대화 로그, 고객 문의 이력, Slack 스레드, 이슈 트래커...
→ 구조 없음, 중복 많음, 노이즈 섞임, 같은 정보가 다양한 표현으로 반복
```

이런 데이터에서 정확한 답변을 뽑아내야 한다면 어떻게 될까?

### 왜 이 시나리오인가

비구조화 데이터 환경에서는 10주 커리큘럼에서 배운 고급 전략들이 비로소 제 역할을 한다:

| 전략 | FAQ에서 | 채팅 로그에서 |
|------|--------|-------------|
| **Baseline 벡터 검색** | 충분히 잘 됨 | 노이즈 청크가 상위에 올라옴 |
| **ReRanker** | 역효과 (대형 청크 잘라냄) | 노이즈 걸러내는 데 효과적 |
| **QueryTransform** | 미미한 개선 | 다양한 표현 커버에 실질적 효과 |

ReRanker가 왜 존재하는지, QueryTransform이 언제 의미 있는지를 수치로 확인할 수 있다.

---

## 구체적으로 무엇을 만들면 되는가

### Step 1. 데이터 준비

실제 채팅 로그 또는 유사 형태의 데이터를 준비한다.
없다면 직접 생성해도 된다 — ChatGPT로 "초록 코퍼레이션 고객 문의 대화 100건"을 만들어도 충분하다.

```
데이터 예시:
고객: 어제 주문했는데 배송 언제 와요?
상담사: 주문 번호 알려주시면 확인해드릴게요.
고객: 20240315-001이요
상담사: 오늘 오후 발송 예정이고 내일 도착 예정입니다.
---
고객: 환불 하고 싶어요
상담사: 구매일로부터 30일 이내라면 환불 가능합니다...
```

### Step 2. 청킹 전략 재설계

토큰 기준으로 자르면 대화 맥락이 깨진다. 새로운 청킹이 필요하다.

**시도해볼 것들:**
- 대화 턴(turn) 단위로 자르기
- 주제 전환 감지 후 자르기 (semantic chunking)
- 슬라이딩 윈도우로 대화 연속성 유지

Week 02에서 배운 청킹 전략들을 이 데이터에 다시 적용하고 비교해보자.
FAQ에서는 효과 차이가 미미했지만, 여기서는 전략 선택이 결과를 크게 바꾼다.

### Step 3. 평가 기준 재설계

FAQ는 `expected_answer`가 명확했다. 채팅 로그에서는 ground truth 구성 자체가 도전이다.

**고려할 것들:**
- 정답 셋을 어떻게 만들 것인가 (수동 레이블링, LLM 보조 레이블링)
- Week 04의 LLM 판정 방식을 그대로 쓸 수 있는가
- RAGAS 라이브러리(Python)를 Java와 병행해서 쓰는 방법

### Step 4. 검색 전략 비교 재실험

Week 04와 동일한 비교 실험을 새 데이터로 반복한다.

```
기대 결과:
Baseline    < ReRanker  ← 이번엔 뒤집힌다
Baseline    < QueryTransform
지연시간 증가 < 정확도 향상
```

Week 04 EVALUATION.md와 나란히 놓고 비교하면,
"왜 같은 전략이 다른 결과를 내는가"가 데이터 구조의 차이에서 온다는 것을 수치로 확인할 수 있다.

---

## 그 외 탐구할 방향들

### 청킹을 Q&A 단위로 바꾸면?

10주 커리큘럼 안에서도 바로 해볼 수 있는 실험이다.
현재 FAQ를 `TokenTextSplitter` 대신 마크다운 헤더 기준(`###`)으로 쪼개면:

```
17개 대형 청크 → ~100개 Q&A 단위 청크
```

이 상태에서 Week 04 전략들을 다시 측정해보자.
ReRanker의 Top-1이 Baseline을 뛰어넘는지 확인할 수 있다.
Week 04 EVALUATION.md의 분석 섹션에서 예측한 것이 맞는지 검증하는 실험이다.

### RAGAS 도입

Week 04에서 LLM 판정 방식을 직접 구현했다.
Python 생태계에는 이미 검증된 RAGAS 라이브러리가 있다.

Java 서비스와 Python 평가 파이프라인을 분리해서 연동하는 구조를 만들어보면,
실무 MLOps 파이프라인의 기초를 경험할 수 있다.

### 하이브리드 검색

Week 04 미션에 "하이브리드 검색"이 언급됐지만 실제로 구현하지 않았다.
벡터 검색 + 키워드 검색(BM25)을 결합하고 RRF(Reciprocal Rank Fusion)로 병합하면
특히 고유명사, 모델명, 코드명처럼 의미보다 형태가 중요한 쿼리에서 효과가 크다.

Qdrant는 sparse vector를 지원하므로, Week 03에서 구축한 인프라 위에 바로 올릴 수 있다.

### 스트리밍 응답

10주 커리큘럼의 모든 응답은 완성될 때까지 기다린다.
실제 챗봇 UX에서는 스트리밍이 기본이다.

Spring AI의 `stream()` API를 사용해 SSE(Server-Sent Events)로 응답을 내보내는 구조로 바꿔보자.
Week 05에서 구축한 OTel 트레이싱이 스트리밍 환경에서도 동작하는지 확인하는 것도 흥미로운 문제다.

---

## 참고

- Week 04 실험 결과: [week04/EVALUATION.md](week04/EVALUATION.md)
- 청킹 전략 비교: [week02/notes/](week02/notes/)
- 관련 이슈:
  - [#6 FAQ 규모 확대](https://github.com/jaeyeonling/spring-ai-bootcamp/issues/6)
  - [#12 미션 수치 하드코딩 제거](https://github.com/jaeyeonling/spring-ai-bootcamp/issues/12)
